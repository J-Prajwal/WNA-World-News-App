const init = {
    isLoading: false,
    isError: false,
    userDetails: []
}

export const authReducer = (state = init, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
